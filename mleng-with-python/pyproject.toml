[tool.poetry]
name = "mleng-with-python"
version = "0.1.0"
description = ""
authors = ["4figuer"]
readme = "README.md"
packages = [{include = "mleng_with_python"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
